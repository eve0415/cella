# cella-config

- Changing schema codegen: run `cargo insta review` after changes
- JSONC preprocessor preserves byte offsets — do not use string replacement on raw JSON
- Config merge tests use snapshot assertions
