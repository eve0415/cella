# cella-protocol

IPC protocol types shared between `cella-agent`, `cella-daemon`, and `cella-port`.

Contains the control socket message definitions (newline-delimited JSON)
and shared credential field parsing/formatting utilities.
