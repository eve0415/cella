//! Container target resolution.
//!
//! Re-exports from `cella_backend::resolve` for backward compatibility.
//! New code should import from `cella_backend` directly.

pub use cella_backend::resolve::ContainerTarget;
